{% extends 'base_back.html.twig' %}
{% block body %}
    <div class="sidebar-search">

        <div class="input-group custom-search-form">

            <input type="text" id="search" class="form-control pl-3" placeholder="Search here">
        </div>
        <!-- /input-group -->
    </div>
    <div class="container">
        <div class="row">
            <div class="col-sm-12 text-center breadcrumbs_inner">
                <table class="table" border="1" style="color:#22242a ;border: #a7a7a7">
                    <h4><i class="fa fa-angle-right"></i> La liste des chéquier</h4>
                    <hr>
                    <thead>
                    <tr>
                        <th>Motif</th>

                        <th>Date
                                <a href="{{ path('croissant') }}"><img src="{{ asset('img.png') }}" height="20" width="20"> </a>

                                <a href="{{ path('decroissant') }}"><img src="{{ asset('img_1.png') }}" height="20" width="20"> </a>

                        </th>



                        <th></th>

                    </tr>
                    </thead>
                    <tbody>

                    <tr>
                        {% for U in chequier%}
                    <tr>

                        <td>
                            {{ U.motifchequier }}
                        </td>

                        <td>
                            {{ U.datecreation |date('d/m/Y')}}
                        </td>


                        <td>


                            <a href="{{ path('suppE',{'id':U.id}) }}">
                                <img src="{{ asset('delete2.png') }}"  style="width:42px;height:42px;border:0">
                            </a>

                        </td>



                    </tr>
                    {% endfor %}


                    </tbody>
                </table>
            </div>
            <!-- /content-panel -->
        </div>
        <!-- /col-md-12 -->
    </div>
    </div>
{% endblock %}
